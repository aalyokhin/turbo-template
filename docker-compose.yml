services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/app.db
      - WEB_DIST_PATH=/app/web-dist
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
